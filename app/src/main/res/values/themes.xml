<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MyLittlePokemons" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyLittlePokemons" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/yellow</item>
        <item name="colorPrimaryDark">@color/dark_yellow</item>
        <item name="titleTextColor">@color/black</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="SpecialItems" parent="Theme.MyLittlePokemons">
        <item name="android:background">@color/special_item</item>
    </style>
</resources>